// Local-only configuration example.
// Copy this file to config.js if you later wire TradeFlow to a local configuration loader.
//
// IMPORTANT:
// A browser-side API key is visible to anyone using the page.
// For a real public deployment, use a backend/serverless proxy.
window.TRADEFLOW_CONFIG = {
  twelveDataApiKey: ""
};
